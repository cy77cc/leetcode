package main

import "fmt"

func convert(s string, numRows int) string {
	bytes := []byte(s)
	length := len(bytes)
	result := make([]byte, length)
	for i := 0; i < length; i++ {

	}

}

func main() {
	convert("", 10)
}
